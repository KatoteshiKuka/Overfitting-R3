/**
 * Feature 2 — area del personale di struttura.
 *
 * Volutamente vuota: la sviluppa il team. Chi la prende in carico parte da qui e
 * dai dati di `GET /api/v1/congestion`, che sono già scrivibili per essere aggiornati
 * dagli operatori (vedi `backend/app/features/congestion/models.py`).
 */
export function OperatorePage() {
  return <div aria-label="Area operatori, in costruzione" />;
}
