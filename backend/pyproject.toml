[project]
name = "presidio-backend"
version = "0.1.0"
description = "HealthPulse — Emergency Triage Assistant, API"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]==0.115.6",
    "pydantic==2.10.4",
    "pydantic-settings==2.7.0",
    "sqlalchemy==2.0.36",
    "uvicorn==0.34.0",
]

[dependency-groups]
dev = ["ruff==0.8.6"]

[tool.uv]
package = false
